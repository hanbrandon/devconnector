const initialState = {
    profile: null,
    profiles: [],
    repost: [],
    loading: true,
    error: {}
}

export default function(state = initialState, action ) {
    const { type, payload } = action;

    switch(type) {
        case
    }

}